/events:
  get:
    tags:
      - Events
    summary: Retrieve a list of all events
    responses:
      '200':
        description: A successful response returning an array of events.
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/Event'
  post:
    tags:
      - Events
    summary: Create a new event
    security:
      - googleAuth: []
      - bearerAuth: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Event'
    responses:
      '201':
        description: Event created successfully.

/events/{id}:
  put:
    tags:
      - Events
    summary: Update an existing event
    security:
      - googleAuth: []
    parameters:
      - in: path
        name: id
        schema:
          type: string
        required: true
        description: The unique MongoDB ID of the event to update.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Event'
    responses:
      '200':
        description: Event updated successfully.
  delete:
    tags:
      - Events
    summary: Delete an event
    security:
      - googleAuth: []
      - bearerAuth: []
    parameters:
      - in: path
        name: id
        schema:
          type: string
        required: true
        description: The unique MongoDB ID of the event to delete.
    responses:
      '200':
        description: Event deleted successfully.